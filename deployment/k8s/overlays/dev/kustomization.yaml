apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Example patches for dev can go here (e.g. lower resources, different alias).
# patchesStrategicMerge:
#   - patch-configmap.yaml
