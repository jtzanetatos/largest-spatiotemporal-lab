# src/

Python package source code for this project.

- `spatiotemporal_lab/` is the importable package (rename via cookiecutter).
- Training entrypoints live in `spatiotemporal_lab/cli/`.
- Deployment (FastAPI, Docker, K8s, Triton) lives under `deployment/` (not here).
